#![cfg(test)]

/// Placeholder portability test.
///
/// The prior file contained markdown/frontmatter text and did not parse as Rust,
/// causing `cargo fmt`/pre-commit failures.
#[test]
fn portability_smoke_placeholder() {
    assert!(true);
}
